<div class="postsAll">
    <p>author: <%= post.user.login %></p>
    <%= image_tag(post.image.large.url, alt: 'Image') if post.image?%>
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Read more..", post_path(post), :class => 'btn btn-outline-primary'%>
    </div>
