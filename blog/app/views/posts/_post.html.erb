<div class="postsAll">
    <p>author: <%= post.user.login %></p>
    <% if post.images? %>
  <ul>
    <% post.images.each do |images| %>
      <li><%= image_tag(images.large.url, alt: 'Image')%></li>
    <% end %>
  </ul>
<% end %>
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Read more..", post_path(post), :class => 'btn btn-outline-primary'%>
    </div>
