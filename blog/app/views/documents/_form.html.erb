<%= form_with(model: document, local: true) do |form| %>
  <% if document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
        <% document.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :path %>
    <%= form.text_area :path %>
  </div>

  <div class="field">
    <%= form.label :documentable_id %>
    <%= form.number_field :documentable_id %>
  </div>

  <div class="field">
    <%= form.label :documentable_type %>
    <%= form.text_field :documentable_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
